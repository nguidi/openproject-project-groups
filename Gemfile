# frozen_string_literal: true

source "https://rubygems.org"

# Resolve dependencies from the gemspec. In the OpenProject dev stack this gem is
# referenced from the host's Gemfile.plugins (path:), so this Gemfile is only used
# when working on the plugin in isolation.
gemspec
